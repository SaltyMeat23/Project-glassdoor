# ContractIQ platform — local secrets. Copy to `.env.local` (git-ignored) and fill in.
# Used by the Neon migration + any Postgres-targeted scripts.
# Get the connection string from neon.tech → your project → Connection string.

DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-1.aws.neon.tech/neondb?sslmode=require
